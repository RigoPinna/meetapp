import React from 'react'
import { Image } from 'react-native';

export const IconJoinGroup = ({ pathColor }) => {
    return (
        <Image source={require('../../../assets/Profile.png')}/>
  );
}
