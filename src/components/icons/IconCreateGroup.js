import React from 'react'
import { Image } from 'react-native';

export const IconCreateGroup = ({ pathColor }) => {
    return (
        <Image source={require('../../../assets/Bookmark.png')}/>
  );
}
